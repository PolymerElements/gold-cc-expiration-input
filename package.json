{
  "name": "@polymer/gold-cc-expiration-input",
  "private": true,
  "description": "A validating input for a credit card expiration date",
  "repository": {
    "type": "git",
    "url": "git://github.com/PolymerElements/gold-cc-expiration-input.git"
  },
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@polymer/gen-typescript-declarations": "^1.1.1",
    "bower": "^1.8.0"
  },
  "scripts": {
    "update-types": "bower install && gen-typescript-declarations --deleteExisting --outDir ."
  }
}
